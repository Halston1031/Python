Dictionary
    ▧  字典(Dictionary) 是另一種可變容器模型，且可儲存任意型別物件。
         由鍵值對組成的集合，通常使用key來訪問數據。
         格式：d = {key1 : value1, key2 : value2 }
         特點： ▣  無下標概念，是一個無序的鍵值集合
                   ▣  用"花括號 {}"表示字典對象，每個鍵.值(key.value)用"冒號 : "分隔，每個鍵值對之間用逗號分隔
                   ▣  key必須是不可變的類型【元組、字符串】
                   ▣  value 可以是任意類型
                   ▣  key 必定唯一，若存在重複的鍵，後者會覆蓋前者
     ▧ lambda函數也叫匿名函數，及即沒有具體名稱的函數，它允許快速定義單行函數，類似於C語言的宏，可以用在任何需要函數的地方。
         格式：
            lambda 變量 : 要執行的語句
            lambda [arg1 [, agr2,.....argn]] : expression
            
         lambda與def的區別：
         ▣  def創建的方法是有名稱的，而lambda沒有。
         ▣  lambda會返回一個函數對象，但這個對像不會賦給一個標識符，而def則會把函數對象賦值給一個變量（函數名）。
         ▣  lambda只是一個表達式，而def則是一個語句。
         ▣  lambda表達式” : “後面，只能有一個表達式，def則可以有多個。
         ▣  像if或for或print等語句不能用於lambda中，def可以。
         ▣  lambda一般用來定義簡單的函數，而def可以定義復雜的函數。
         ▣  lambda函數不能共享給別的程序調用，def可以。
         

